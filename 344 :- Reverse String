class Solution {
public:
    void reverseString(vector<char>& s) {
        int size = s.size();
        int r=0;
        int l = size -1;
        while(r<l){
            swap(s[r],s[l]);
            r ++;
            l --;
        }
        return;
        
    }
};
